#include <stdio.h>

int main()
{
    int luckynumbers[] = {2, 3, 4, 6, 7, 5 };
    int somenumbers[20];
    somenumbers[0] = 80;
    printf("%d\n", luckynumbers[2]);
    printf("%d", somenumbers[0]);
    return(0);
}